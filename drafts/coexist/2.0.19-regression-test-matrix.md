# Co-Exist 2.0.19 regression test matrix (everything since live 2.0.9)

Scope: `git diff 7b7ce46..origin/main` = 21 commits, 73 files. Live stores run 2.0.9 (iOS build 69 / Android vc56); main is the 2.0.19 line. Test EVERY change on three surfaces: web, iOS simulator, Android emulator. Pass = observed behaviour, not narration; capture a screenshot per assertion.

## Surfaces + how to drive
- WEB: app.coexistaus.org (Vercel prod, current main). Drive via the canonical CDP (`bash /Users/ecodia/.code/ecodiaos/backend/scripts/chrome-cdp.sh` then attach :9222), mobile viewport 390x844 for app pages. Log in with the Co-Exist app test account (docs/secrets/coexist-app-test.md -> kv_store.creds.coexist {url,email,password}).
- iOS SIM: boot an available sim (e.g. iPhone 17 Pro 61DC6FE4). Build: `npm run build && npx cap sync ios` then xcodebuild for the simulator sdk (Debug, no signing needed for sim), install the .app via `xcrun simctl install booted <app>`, launch. Drive with Maestro (`~/.maestro/bin/maestro test`) + idb/simctl for the new flows. Focusless per own-your-simulator + sim-driving-must-be-focusless.
- ANDROID EMULATOR: `export PATH=$PATH:~/Library/Android/sdk/platform-tools:~/Library/Android/sdk/emulator`. Boot an AVD (create one if none: pixel + a recent + an OLD API for the WebView-floor check). Build: `npx cap sync android && cd android && ./gradlew assembleDebug`, install the apk via adb, drive with Maestro.
- Maestro suite: 65 flows in .maestro/flows/ cover general regression. Run the whole suite on BOTH sims. The NEW ticketing flows below do NOT exist yet - author them (or drive via idb/coord-tap + adb) and add to the suite.

## Leader account (BLOCKER to resolve FIRST)
The export + create-event-questions flows need a leader/staff account; the test account is a "regular user". Step 1: check the test account's role; if not staff, grant it leader on a throwaway test collective in the coexist DB (project tjutlbzekfouwsiaplbr) so the leader flows are drivable. Record what you did.

## Test matrix (each row: all 3 surfaces unless noted)

### A. Custom-questions + export feature (the headline change)
1. Create ticketed event with custom questions (LEADER): create-event -> Ticketing step -> Require tickets -> add a tier -> add Attendee questions (one of each type: short_text, boolean, single_select, multi_select; mark one required) -> publish. Verify the questions save (event_ticket_questions rows).
2. Buy ticket AUTHED with questions: open that event -> Get ticket -> the TicketQuestionsModal appears -> required gate blocks empty -> fill -> continue -> Stripe (use test mode / a $0 tier to avoid real charge). Verify custom_answers persisted on the ticket.
3. Buy ticket GUEST with questions: public /event/:id in a logged-out browser -> buy card -> questions modal -> answers -> guest-ticket-checkout. Verify persistence.
4. Comprehensive export (LEADER): event page -> Attendees export -> All/Going/Waitlisted/Cancelled filter -> a column per question -> Download .csv + Copy text + Copy phone list. Verify the CSV carries name/status/phone/dietary/medical/emergency + answer columns.
5. Dietary + medical required at purchase (campout): buy a campout ticket without profile dietary/medical -> the CampoutRequirementsModal blocks + captures. Verify.

### B. Grey-zone + seat fixes (this session)
6. Onboarding one-tap RSVP excludes ticketed events: a fresh user in onboarding sees only non-ticketed events on the first-event step (no ticketed campout with a Going button).
7. Register guard: attempting a bare registration on a ticketed event is refused ("This event needs a ticket").
8. Roster seats a confirmed ticket as Going: a paid-but-waitlisted user shows under Going/expected, not waitlist (getEventRoster).

### C. Cross-cutting
9. Hero overscroll (overscrollBackdrop): event + collective detail hero continues into the native overscroll gap, no white gap, on iOS + Android (native only).
10. Spectral attribution font: the "built by Ecodia" mark renders in upright Spectral (not EB Garamond), web + native footers.
11. Event detail prep-and-access pills read from event_extras jsonb.
12. RLS: a collective leader can read their event registrants' profiles (drives the export); a non-leader cannot.
13. Sentry: native crash capture + edge-fn withSentry. Force a caught error path if feasible and confirm it lands in the Co-Exist Sentry project (note: the standing P1 says Sentry canaries are dead on sibling apps - verify Co-Exist's project actually receives).

### D. General regression (Maestro 65-flow suite, BOTH sims)
Run the full suite: sign-in, tabs, admin-walk, settings, profile, tickets, explore, notifications, collectives, chat, shop, tasks, updates, learn, referral, check-in, carpool, etc. Any flow that regressed since 2.0.9 is a fail. Old-Android WebView floor: run the app on an OLD API-level emulator (the class-bug guard) and confirm no white-screen crash.

## Report format
Per surface (web / iOS / Android): a pass/fail table for rows 1-13 + the Maestro suite result (X/65), each fail with the screenshot path + the exact observed behaviour. End with a go/no-go for the store submission. File the report at drafts/coexist/2.0.19-test-report.md and update status_board 56fe7049.
